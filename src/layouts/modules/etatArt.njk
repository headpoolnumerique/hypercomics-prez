<section id="{{ url | slugify }}" class=" demo">
  {% include "./prev.njk" %}
  <header>
    <h2>{{ item.data.title }}</h2>
             {# {{item.templateContent | safe}} #}
<a target="_blank" href="{{ item.data.url }}" >open in your browser </a>
  </header>
  <div class="screenshot resizer">
  <iframe loading="lazy" scrolling="yes" resize src="{{item.data.url}}"></iframe>
 </div> 
  {% include "./next.njk" %}
</section>
